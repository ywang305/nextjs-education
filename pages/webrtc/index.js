import React, { useState, useCallback } from 'react';
import Link from 'next/link';
import Box from '@material-ui/core/Box';

const WebRtcPage = () => {
    return <div> waiting for design...</div>;
};

export default WebRtcPage;
